// Re-export from the new shared location for backward compatibility
export {
    AiService,
    type ValidationFeedback,
    type FieldFeedback,
    type LegalityCheckResult,
    type CandidateValidationResult,
} from '../ai/ai.service';
