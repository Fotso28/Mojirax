# Audit : Emails transactionnels Brevo (v2 — approfondi)

**Date** : 2026-03-12
**Fichiers analyses** : 15

| # | Fichier |
|---|---------|
| 1 | `api/src/notifications/email/email.service.ts` |
| 2 | `api/src/notifications/email/email-compiler.service.ts` |
| 3 | `api/src/notifications/email/email.constants.ts` |
| 4 | `api/src/admin/dto/update-email-config.dto.ts` |
| 5 | `api/src/admin/dto/update-push-config.dto.ts` |
| 6 | `api/src/notifications/notifications.module.ts` |
| 7 | `api/src/notifications/notifications.service.ts` |
| 8 | `api/src/app.module.ts` |
| 9 | `api/src/auth/auth.service.ts` |
| 10 | `api/src/auth/auth.module.ts` |
| 11 | `api/src/admin/admin.service.ts` |
| 12 | `api/src/admin/admin.controller.ts` |
| 13 | `api/src/applications/applications.service.ts` |
| 14 | `api/src/unlock/unlock.service.ts` |
| 15 | `api/prisma/schema.prisma` |

---

## Compilation

| Cible | Statut | Erreurs |
|-------|--------|---------|
| API (`api/`) | OK | Aucune (erreur pre-existante `redis.module.ts` non liee) |
| Web (`web/`) | OK | Aucune |

---

## Qualite du code

Corrections appliquees durant cet audit :

| # | Fichier | Probleme | Severite | Fix |
|---|---------|----------|----------|-----|
| 1 | `email.service.ts:117` | `(response as any)?.messageId` — cast `any` inutile, `SendTransacEmailResponse` a `messageId` directement | BASSE | Supprime le cast `as any` |
| 2 | `schema.prisma:808` | `MESSAGE_RECEIVED` manquant dans `enabledTypes` par defaut de `EmailConfig` — les emails de ce type seraient ignores | MOYENNE | Ajoute `MESSAGE_RECEIVED` a la liste |

---

## Securite (OWASP)

| # | Regle | Fichier | Verification | Statut |
|---|-------|---------|-------------|--------|
| 1 | A01 | `admin.controller.ts` | `@UseGuards(FirebaseAuthGuard, AdminGuard)` au niveau controller | OK |
| 2 | A01 | `email.service.ts:54-63` | `select` explicite sur User (email, firstName, name, preferredLang) | OK |
| 3 | A01 | `notifications.service.ts:130` | Ownership verifiee pour `markAsRead` | OK |
| 4 | A02 | `email.service.ts:25` | Brevo API key via `ConfigService` (pas en dur) | OK |
| 5 | A03 | `email-compiler.service.ts:44-48` | Variables echappees via `escapeHtml()` | OK |
| 6 | A03 | `email.service.ts` | Pas de `$queryRawUnsafe`, que Prisma client | OK |
| 7 | A04 | `admin.controller.ts:96` | `UpdatePushConfigDto` avec `@IsEnum` | OK |
| 8 | A04 | `admin.controller.ts:108` | `UpdateEmailConfigDto` avec `@IsEnum` | OK |
| 9 | A04 | `email.service.ts:179` | Cron borne `take: 50` | OK |
| 10 | A04 | `notifications.service.ts:72` | Pagination `Math.min(limit, 20)` | OK |
| 11 | A07 | `auth.service.ts` | `firebaseUser` du token Firebase (pas du body) | OK |
| 12 | A09 | Tous les fichiers | Logger NestJS partout, pas de `console.log` | OK |
| 13 | A09 | `email.service.ts:122` | Log du `user.id` (pas l'email) | OK |
| 14 | A10 | `email.service.ts` | Pas de fetch sur URL utilisateur | OK |

> Aucune faille de securite detectee.

---

## Verification approfondie de l'API Brevo

| Point | Resultat |
|-------|----------|
| `BrevoClient` existe dans `@getbrevo/brevo` | OK — export verifie |
| `sendTransacEmail` methode existe | OK — dans `TransactionalEmailsClient` |
| Signature `{ sender, to, replyTo, subject, htmlContent, tags }` acceptee | OK — types verifies |
| `SendTransacEmailResponse.messageId` existe | OK — `messageId?: string` |
| `HttpResponsePromise<T>` resolve vers `T` | OK — `extends Promise<T>` |
| Appel avec fake key retourne 401 (pas d'erreur de structure) | OK — teste |

---

## Verification de coherence

| Point | Resultat |
|-------|----------|
| `NotificationType` enum : 13 valeurs | OK — 10 originales + WELCOME + ONBOARDING_REMINDER + MESSAGE_RECEIVED |
| `EMAIL_TEMPLATE_MAP` couvre 13 types | OK — tous presents |
| `EMAIL_ACTION_URL_MAP` couvre 13 types | OK — tous presents |
| `enabledTypes` par defaut couvre 13 types | OK (apres fix) |
| 14 templates FR + 14 EN existent | OK — verifies (system.mjml sert aussi MESSAGE_RECEIVED) |
| Layout MJML a `{{content}}` et `{{footerText}}` | OK |
| i18n fr.json a 14 sujets + labels | OK |
| i18n en.json a 14 sujets + labels | OK |
| `resolveTemplateName` gere MODERATION_ALERT sous-types | OK — via `data.moderationStatus` |
| `resolveActionUrl` gere PROFILE_UNLOCKED candidate/project | OK — via `data.unlockType` |
| `escapeHtml` protege contre injection HTML | OK |
| Fire & forget pattern coherent push/email | OK — `.catch()` partout |
| ONBOARDING_REMINDER cree notif in-app + email | OK — `prisma.notification.create` + `sendEmail` |
| WELCOME ne cree pas de notif in-app | OK — appel direct `sendWelcome()` |
| AuthModule importe NotificationsModule | OK |
| ScheduleModule.forRoot() dans AppModule | OK |
| nest-cli.json copie assets MJML/JSON | OK |
| ApplicationsService passe `projectSlug` + `candidateName` | OK |
| UnlockService passe `targetName` + `unlockType` | OK |

---

## Resume fonctionnel

### Architecture

```
notify(userId, type, title, message, data)
  |-- Create Notification in DB
  |-- sendPush() [PushService] (fire & forget)
  |-- sendEmail() [EmailService] (fire & forget)
       |-- Check EmailConfig (enabled + type)
       |-- Fetch User (email, preferredLang)
       |-- Resolve template + actionUrl + variables
       |-- Compile MJML -> HTML (cached layout)
       |-- Send via Brevo API
       |-- Log to EmailLog
```

### Endpoints

| Methode | Path | Guard | Description |
|---------|------|-------|-------------|
| GET | /admin/email-config | FirebaseAuthGuard + AdminGuard | Config email |
| PATCH | /admin/email-config | FirebaseAuthGuard + AdminGuard | Modifier config email |

### Modeles Prisma

- `User` : +`preferredLang` (String, defaut "fr"), +`emailLogs` (relation)
- `EmailConfig` (nouveau) : Singleton admin
- `EmailLog` (nouveau) : Tracking emails (userId, type, status, brevoId)
- `NotificationType` : +WELCOME, +ONBOARDING_REMINDER, +MESSAGE_RECEIVED

---

## Correctifs appliques dans cet audit

| # | Fichier:Ligne | Correction | Severite |
|---|--------------|-----------|----------|
| 1 | `email.service.ts:117` | Supprime cast `(response as any)` — type natif `SendTransacEmailResponse` a deja `messageId` | BASSE |
| 2 | `schema.prisma:808` | Ajoute `MESSAGE_RECEIVED` a `enabledTypes` par defaut de `EmailConfig` | MOYENNE |

---

## Score

| Categorie | Score | Commentaire |
|-----------|-------|-------------|
| Qualite | 9/10 | Architecture propre, bonne separation des responsabilites, Logger NestJS, DTOs valides, select explicites, pagination bornee, echappement HTML. |
| Securite | 10/10 | Guards presents, pas de secrets en dur, echappement HTML, DTOs class-validator, API Brevo verifiee. |
| **Global** | **9/10** | Implementation solide. Les 2 corrections mineures ont ete appliquees. |
